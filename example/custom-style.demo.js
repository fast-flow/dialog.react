var React = require('react')
var Dialog = require('dialog.react').default
var App = React.createClass({
    render: function () {
        return (
            <Dialog className="alert--info" >basic</Dialog>
        )
    }
})
module.exports = App
